import 'package:flutter/material.dart';

class TeacherPlanPage extends StatelessWidget {
  const TeacherPlanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Lehrerpläne");
  }
}
