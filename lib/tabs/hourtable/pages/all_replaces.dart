import 'package:flutter/material.dart';

class AllReplacesPage extends StatelessWidget {
  const AllReplacesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Alle Vertretungen");
  }
}
