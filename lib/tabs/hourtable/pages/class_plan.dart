import 'package:flutter/material.dart';

class ClassPlanPage extends StatelessWidget {
  const ClassPlanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Klassenpläne");
  }
}
